LAW_VERSION: 1.0

PANEL_0_INTENT:
  UI:
    - ACK_CHECKBOX: required
  EFFECT:
    - No schema fields written
    - Blocks progression until acknowledged

PANEL_1_ASSET:
  UI:
    - TEXT_NAME
    - TEXT_SYMBOL
  WRITES:
    ASSET.NAME
    ASSET.SYMBOL
  CONSTRAINTS:
    - Enforce length and charset per schema

PANEL_2_SUPPLY:
  UI:
    - RADIO_SUPPLY_TYPE
    - SLIDER_TOTAL_SUPPLY
    - SLIDER_MAX_SUPPLY (conditional)
  WRITES:
    SUPPLY.TYPE
    SUPPLY.TOTAL_SUPPLY
    SUPPLY.MAX_SUPPLY
  RULES:
    - MAX_SUPPLY enabled ONLY if TYPE == CAPPED
    - TOTAL_SUPPLY == MAX_SUPPLY if TYPE == FIXED
    - No mint authority UI exists

PANEL_3_BURN_TIME:
  UI:
    - TOGGLE_BURN_MODE
    - SLIDER_BURN_CAP (conditional)
    - TOGGLE_TIMELOCK
  WRITES:
    BURN.MODE
    BURN.CAP_PERCENT
    TIMELOCK.ENABLED
  RULES:
    - CAP_PERCENT visible ONLY if MODE == AUTOMATED

PANEL_4_TIMELOCK_SCHEDULE:
  UI:
    - FIXED_SCHEDULE_SELECTOR (read-only)
  WRITES:
    TIMELOCK.SCHEDULE
  RULES:
    - Schedule values are immutable constants

PANEL_5_DISTRIBUTION:
  UI:
    - SLIDER_LP_PERCENT
    - SLIDER_AIRDROP_PERCENT
    - SLIDER_SAVINGS_PERCENT
  WRITES:
    DISTRIBUTION.LP_PERCENT
    DISTRIBUTION.AIRDROP_PERCENT
    DISTRIBUTION.SAVINGS_PERCENT
  RULES:
    - Sliders auto-normalize
    - SUM must equal 100 before progression

PANEL_6_REVIEW:
  UI:
    - READ_ONLY_SUMMARY
    - WARNING_DISPLAY
  EFFECT:
    - No schema mutation permitted

PANEL_7_DEPLOY:
  UI:
    - HOLD_TO_CONFIRM
  EFFECT:
    - Freeze law object
    - Emit canonical LAW object to compiler
