Module: Health Benefit Engine (HBE)

Type: Optional / Post-Scale Only

Inputs:
    - user_stake
    - stake_duration
    - treasury_yield

Outputs:
    - benefit_tier
    - subsidy_amount
    - provider_access

Constraints:
    - cannot activate without protocol-level thresholds
    - payouts capped by yield (not treasury principal)
    - jurisdiction-aware delivery required
